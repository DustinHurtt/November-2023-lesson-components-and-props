import Button from "./Button";

const Navbar = () => {
  return (
    <nav>
        <p>React - Components & props</p>

        <Button />
        <Button />
    </nav>
  );
};

export default Navbar;
